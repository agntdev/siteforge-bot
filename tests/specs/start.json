[
  {
    "name": "/start replies with the welcome + main menu",
    "steps": [
      {
        "send": { "text": "/start" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "👋 Welcome to the Website Code Generator!\n\nI'll ask a few questions about your site, then build a ready-to-run project and send it as a ZIP you can download. Tap a button to get started."
            }
          }
        ]
      }
    ]
  },
  {
    "name": "Back to menu re-renders the welcome",
    "steps": [
      {
        "send": { "callback": "menu:main" },
        "expect": [{ "method": "editMessageText" }]
      }
    ]
  }
]
